Fork from `https://github.com/cferdinandi/tabby`. Use attribute `visibility=hidden` instead of `hidden=hidden` when  tab container is hidden.

Write your CSS to support.

EXAMPLE:
```css
[visibility=hidden]{
	visibility:hidden
}
```